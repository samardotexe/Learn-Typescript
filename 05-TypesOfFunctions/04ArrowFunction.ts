//Arrow Function 
// uses => to defined a function


//They are also called Anonymous Arrow Function
const numSum = (a: number, b: number): number => a+b;

const hi = (name: string): string => `Hello, ${name}`;


//or

const numSum1 = (a: number, b: number): number => {
    return a+b;
}

